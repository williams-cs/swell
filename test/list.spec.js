"use strict";
/*
import { assert,expect } from 'chai';
import 'mocha';
import { ListNode } from '../lib/lists/ListNode';
import { Return, VariableNode, PlusOp, NumberNode, AssignOp, Scope, SequenceNode } from '../lib';
import { ListHead } from '../lib/lists/ListHead';

//[0,1,2,3]

describe('A list', () => {
    it('should evaluate to [0,1,2,3]', () => {
        let list3 = new ListNode(new NumberNode(3),null);
        let list2 = new ListNode(new NumberNode(2),list3,list1head.id);
        let list1 = new ListNode(new NumberNode(1),list2,list1head.id);
        var list1head = new ListHead(new NumberNode(0),list1);
        const output = list1.eval(new Scope(null));
        expect(output).to.deep.equal([0,1,2,3]);
    });
    */
/*
it('should evaluate to ',() => {
    const list2 = new ListNode(new NumberNode(2),null);
    const list1 = new ListNode(new NumberNode(1),list2);
    const var1 = new VariableNode("list1");
    const assign1 = new AssignOp(var1,list1);

    const list22 = new ListNode(new NumberNode(4),null);
    const list21 = new ListNode(new NumberNode(3),list22);
    const var2 = new VariableNode("list2");
    const assign2 = new AssignOp(var2,list2);

    const seq1 = new SequenceNode(assign1,assign2);
    const output = seq1.eval(new Scope(null));
    const output1 = seq1.rightVal;
    expect(output1).to.deep.equal([3,4]);
});
*/
/*
});
*/ 
