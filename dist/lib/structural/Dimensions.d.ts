export declare class Dimensions {
    private _x;
    private _y;
    private _radius;
    private _scale;
    constructor(x: number, y: number, scale: number, radius?: number);
    x: number;
    y: number;
    radius: number;
    scale: number;
}
